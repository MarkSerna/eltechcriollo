services:
  # --- API Backend (FastAPI + Playwright) ---
  - type: web
    name: techcriollo-api
    runtime: docker
    rootDir: backend
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "8000"
      - key: AI_PROVIDER
        value: gemini
      - key: RENDER
        value: "true"
      - key: OLLAMA_URL
        value: host.docker.internal:11434
      - key: DATABASE_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: "https://techcriollo-web.onrender.com"

  # --- Web Frontend (React + Vite) ---
  - type: web
    name: techcriollo-web
    runtime: docker
    rootDir: frontend
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: BACKEND_URL
        value: "https://techcriollo-api.onrender.com"
      - key: NGINX_PORT
        value: "80"